#include <stdio.h>
#include <windows.h>

int main(void) {
    printf("fish\n");
    const char *brick[] = {"><(((o>   <o)))><   ><(((o>"};
    for (int i = 0; i < 5; i++) puts(brick[i]);
    return 0;
}
